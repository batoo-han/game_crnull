# -----------------------------------------------------------------------------
# ВАЖНО:
# - Этот файл можно коммитить. Он НЕ содержит секретов.
# - Реальные значения храните в .env (он в .gitignore).
# -----------------------------------------------------------------------------

# -----------------------------
# Общие настройки приложения
# -----------------------------
APP_NAME="game_crnull"
APP_ENV="dev"                   # dev | prod
APP_BASE_URL="http://localhost" # используется в ссылках/логах

# -----------------------------
# Backend (FastAPI)
# -----------------------------
API_HOST="0.0.0.0"
API_PORT="8000"

# Секрет для подписи токенов/куки. В проде должен быть длинным и случайным.
API_SECRET_KEY="CHANGE_ME"

# CORS (через запятую). В проде обычно ваш домен.
API_CORS_ORIGINS="http://localhost:5173, http://localhost:5174"

# -----------------------------
# База данных (PostgreSQL)
# -----------------------------
POSTGRES_HOST="localhost"
POSTGRES_PORT="5432"
POSTGRES_DB="tictactoe"
POSTGRES_USER="tictactoe"
POSTGRES_PASSWORD="CHANGE_ME"

# Альтернатива: единый DSN (если задан, имеет приоритет)
# DATABASE_URL="postgresql+psycopg://user:pass@host:5432/dbname"

# -----------------------------
# Telegram Bot API
# -----------------------------
TELEGRAM_ENABLED="true"
TELEGRAM_BOT_TOKEN="CHANGE_ME"
TELEGRAM_CHAT_ID="CHANGE_ME"
# Необязательный fallback по публичному username группы/канала (например, my_group => @my_group)
TELEGRAM_CHAT_USERNAME=""
# Шаблоны сообщений
TELEGRAM_TEMPLATE_WIN="🎉 Победа! Ваш промокод: {code}\nСпасибо за игру! Делитесь удачей с друзьями."
TELEGRAM_TEMPLATE_LOSE="😔 Сегодня не повезло, но вы молодец!\nПопробуйте ещё раз, удача любит настойчивых."

# -----------------------------
# Админка
# -----------------------------
ADMIN_USERNAME="admin"
# Пароль в открытом виде хранить нельзя. Мы будем хранить ХЭШ в БД.
# Этот пароль используется ТОЛЬКО при первичной инициализации (см. docs).
ADMIN_INITIAL_PASSWORD="CHANGE_ME"

# Дополнительная “маскировка” админки:
# - если задано, админку открывают по URL `/admin/<секрет>`
# - фронтенд передаст секрет в заголовке X-Admin-Route-Secret
# - бекенд будет отклонять любые /api/admin/* запросы без корректного секрета
# Оставьте пустым, если не нужно.
ADMIN_ROUTE_SECRET=""

# -----------------------------
# Промокоды
# -----------------------------
PROMO_TTL_HOURS="72"        # срок действия промокода (по умолчанию)
PROMO_DAILY_LIMIT="500"     # ограничение выдач в сутки (0 = без лимита)

# -----------------------------
# Логирование
# -----------------------------
LOG_LEVEL="INFO"            # DEBUG|INFO|WARNING|ERROR
LOG_DIR="./logs"


